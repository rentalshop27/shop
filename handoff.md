# Handoff: Settings Navigation Refactor (Accordion Submenu)

เอกสารนี้สรุปการเปลี่ยนแปลงล่าสุดที่โฟกัสที่การปรับโครงสร้าง Navigation ของหน้า **Settings** ใหม่ เพื่อให้ใช้งานง่ายขึ้น ลดความซ้ำซ้อนของเมนู และเพิ่มพื้นที่การทำงานสำหรับผู้ใช้

---

## 🎨 1. ฟีเจอร์ใหม่: Accordion Submenu ใน Sidebar
เปลี่ยนโครงสร้างของเมนูตั้งค่าจากเดิมที่เป็น Vertical Tabs ซ้อนอยู่ในหน้า Settings (Nested Layout) มาเป็นการใช้ **Accordion** ซ้อนอยู่ใน Sidebar หลักของระบบเลย

### การปรับปรุง UI/UX ที่สำคัญ
- **Accordion Submenu**: เมนู "ตั้งค่า" ในเมนูหลักสามารถกดเพื่อกางออก (Expand) หรือพับเก็บ (Collapse) ได้ โดยจะแสดงรายการเมนูย่อย เช่น ตั้งค่าทั่วไป, ตัวเลือกสินค้า, สิทธิ์พนักงาน, การแจ้งเตือน, เชื่อมต่อระบบ โดยตรงที่เมนูซ้ายมือ
- **Full Width Content Area**: นำแถบเมนู Settings Sidebar ออกจากส่วนกลางจอ ทำให้เนื้อหาการตั้งค่า (เช่น การจัดการแบรนด์ หมวดหมู่ หรือสี) ได้พื้นที่แสดงผลแบบเต็มหน้าจอ (Full Width) ทำให้รู้สึกกว้างขวางและเหมาะกับการทำงานของ Admin มากขึ้น
- **Professional Icons**: เปลี่ยนการใช้อิโมจิในเมนูตั้งค่า มาใช้ไอคอนจาก `lucide-react` แทน (เช่น `Sliders`, `Tag`, `Users`, `Bell`, `Link`) เพื่อให้ดูเป็นมืออาชีพและเข้ากับดีไซน์ระบบ
- **State Management**: ย้าย state การจัดการเมนูย่อยของ Settings (`settingsSubTab`) ไปไว้ที่ `PrivateApp` (ใน `App.tsx`) เพื่อให้สามารถซิงค์สถานะระหว่าง `SideNav` และ `SettingsPage` ได้อย่างสมบูรณ์ และจำหน้าล่าสุดที่ผู้ใช้เปิดไว้ได้เมื่อสลับกลับมาจากหน้าอื่น

---

## 🌟 2. สิ่งที่หน้า Settings นี้ยังเชื่อมกับของเดิมในระบบ
การทำงานและ Business Rules ต่าง ๆ ของระบบตั้งค่ายังคงเดิม ไม่มีการเปลี่ยน Schema:
- **Global Rental Defaults**: ค่ามาตรฐานเรื่องราคาเช่า มัดจำ และค่าปรับรายวันยังบันทึกผ่านระบบเดิมได้อย่างถูกต้อง
- **Public Catalog Toggle**: สวิตช์เปิด/ปิด public catalog ยังใช้งานได้ปกติ
- **Inventory Option Lists**: การเพิ่ม/ลบแบรนด์ ประเภทชุด และสีหลัก ในหน้า "ตัวเลือกสินค้า" ยังใช้งานและบันทึก State ได้ปกติโดยไม่มีผลกระทบจากการที่หน้าจอถูกเปลี่ยน Layout

---

## 🚀 ถัดไป (Next Steps & Actions Required)
- [ ] ทดสอบ UI เมนูตั้งค่าแบบ Accordion ในมือถือ (Mobile View) เพื่อให้แน่ใจว่าเมนูสามารถแสดงผลและกาง/พับได้อย่างถูกต้อง
- [ ] ตรวจสอบว่าฟอร์มตั้งค่าต่าง ๆ (การเพิ่มสี, แบรนด์, แพ็กเกจเช่า) ใช้งานได้ปกติบน Layout แบบ Full-width
- [ ] (Phase ถัดไป) เตรียมเริ่มพัฒนาหน้าระบบจัดการ **สิทธิ์พนักงาน (Staff Management)**, **การแจ้งเตือน (Notifications)** และ **เชื่อมต่อระบบ (Integrations)** ซึ่งตอนนี้เตรียมเมนูและ Icon รอรับไว้ใน Sidebar เรียบร้อยแล้ว
